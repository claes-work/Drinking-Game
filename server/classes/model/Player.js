class Player {

    constructor (
        uid,
        username,
        color,
        diceCount,
    ) {
        this.uid       = uid;
        this.username  = username;
        this.color     = color;
        this.diceCount = diceCount
    }
}
module.exports = Player
